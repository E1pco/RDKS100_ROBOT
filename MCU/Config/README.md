# Hard Sync Configuration

`rdks100_hard_sync_config.c` contains the current one-camera/one-LiDAR default
configuration.

Board pin/channel names are intentionally kept in `Target/rdks100` so that
configuration can stay focused on signal timing.
